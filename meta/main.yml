---
dependencies:
  - { role: gabethecabbage.elrepo, when: (ansible_os_family == "RedHat") and (ansible_distribution_major_version == "7") }
  
galaxy_info:
  author: gabethecabbage
  description: Ensure a traditional desktop enviroment (gnome 2/mate) is installed on RHEL/Ubuntu
  license: BSD
  min_ansible_version: 2.0
  platforms:
    - name: EL
      versions:
        - 6
        - 7
    - name: Ubuntu
      versions:
        - xenial
        - yakkety
        - zesty
        - artful
  galaxy_tags:
    - desktop
    - mate
    - gnome2
...
